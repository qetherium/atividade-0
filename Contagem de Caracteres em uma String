#include <stdio.h>

int main(){
    char texto[1000] = {0};
    int cont = -1, i = 0;

    fgets(texto, 999, stdin);

    for(i=0; texto[i] != '\0'; i++){
        if(texto[i] =! '\0') cont++;
    }

    printf("%d\n", cont);

    return 0;
}
